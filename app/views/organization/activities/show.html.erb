<%= render 'shared/breadcrumbs_mypage', { title: "アクティビティ詳細" } %>

<div class="row">
  <div class="col-lg-12">
    <div id="add-listing">
      <!-- Section -->
      <div class="add-listing-section padding-top-40">

        <!-- Row -->
        <div class="row with-forms margin-bottom-20">
          <!-- Table-->
          <table class="basic-table">
            <tbody>
              <tr>
                <td data-label="項目"><%= Activity.human_attribute_name("name") %></td>
                <td data-label="内容"><%= @activity.name %></td>
              </tr>
              <tr>
                <td data-label="項目"><%= Activity.human_attribute_name("address") %></td>
                <td data-label="内容"><%= @activity.address %></td>
              </tr>
              <tr>
                <td data-label="項目">URL</td>
                <td data-label="内容"><%= link_to nil, activity_url(@activity) %></td>
              </tr>
              <tr>
                <td data-label="項目"><%= Activity.human_attribute_name("description") %></td>
                <td data-label="内容"><%= safe_join(@activity.description.split("\n"), tag(:br)) %></td>
              </tr>
              <tr>
                <td data-label="項目">組織</td>
                <td data-label="内容"><%= @activity.organization.name %></td>
              </tr>
            </tbody>
          </table>
        </div>
        <!-- Row / End -->

        <!-- Row -->
        <div class="row with-forms margin-bottom-20">
          <!-- Map -->
          <div class="col-md-12">
            <div id="map-location-display" class="margin-top-25 margin-bottom-25" data-lat="<%= @activity.lat %>" data-lng="<%= @activity.lng %>"></div>
          </div>
        </div>
        <!-- Row / End -->

        <!-- Row -->
        <% if @activity.images.attached? %>
          <div class="row with-forms margin-bottom-20">
            <!-- Images preview -->
            <div class="col-md-12">
              <div id="preview">
                <% @activity.images.each do |image| %>
                  <%= image_tag image.variant(resize: '300x200').processed %>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
        <!-- Row / End -->

        <%= link_to "編集", edit_organization_activity_path, class: "button margin-top-40" %>
        <%= link_to "このアクティビティを削除", organization_activity_path, method: :delete, data: { confirm: "削除すると復旧はできません。よろしいですか？" }, class: "border button" %>

      </div>
      <!-- Section / End -->
    </div>
  </div>
